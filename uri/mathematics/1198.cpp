# include <bits/stdc++.h>

using namespace std;

int main(){
    
    long long int a, b, res;
    
    while(scanf("%lld %lld", &a, &b) != EOF){
        res = abs(a-b);
        printf("%lld\n", res);
        
        
    }
    return 0;
}