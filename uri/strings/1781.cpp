#include <bits/stdc++.h>

using namespace std;

int main(){
	
	int n, op;
	string str, str_op;
	scanf(" %d", &n);
	
	for(int i = 0; i < n; ++i){
		cin >> str;
		scanf(" %d", &op);
		while(op--){
			cin >> str_op;
		}
	}
	
	return 0;
}